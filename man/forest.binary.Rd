% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_binary.R
\name{forest.binary}
\alias{forest.binary}
\title{Contour-Enhanced Forest Plot for Binary Outcomes (OR or RR)}
\usage{
forest.binary(
  dat,
  measure = "OR",
  method = "REML",
  xlab = "",
  title = NULL,
  model = "Random-effects",
  estimator = "REML",
  nc_col = "n_c",
  ne_col = "n_t",
  event_c_col = "events_c",
  event_t_col = "events_t",
  diamond.col = "red",
  study.col = "blue",
  CI.col = "blue",
  Pred.Inter.col = "black",
  square.size = 10,
  contour_fill = c("gray95", "gray80", "gray60", "gray40"),
  text_size = 3.5,
  xlim = c(-1.7, 3.5),
  pred = TRUE,
  xpos = list(EventsT = -0.9, EventsC = -0.3, Effect = 2.6, Weight = 3.1),
  study_x = -1.5,
  hetero_x = -1.7
)
}
\arguments{
\item{dat}{A data frame containing the study-level data for meta-analysis.}

\item{measure}{Character string specifying the effect size measure. Options: "OR" (Odds Ratio) or "RR" (Risk Ratio). Default is "OR".}

\item{method}{Method for pooling effect sizes. Typically "REML", "DL", or "ML". Default is "REML".}

\item{xlab}{Label for the x-axis. Default is an empty string.}

\item{title}{Plot title. Default is NULL (no title).}

\item{model}{Character string specifying model type: "Random-effects" or "Fixed-effects". Default is "Random-effects".}

\item{estimator}{Estimator for between-study variance. Default is "REML".}

\item{nc_col}{Column name in \code{dat} for number of participants in the control group.}

\item{ne_col}{Column name in \code{dat} for number of participants in the experimental/treatment group.}

\item{event_c_col}{Column name in \code{dat} for events in the control group.}

\item{event_t_col}{Column name in \code{dat} for events in the treatment group.}

\item{diamond.col}{Color for the summary effect diamond. Default is "blue".}

\item{study.col}{Color for individual study points. Default is "black".}

\item{CI.col}{Color for confidence interval lines. Default is "black".}

\item{Pred.Inter.col}{Color for prediction interval lines. Default is "red".}

\item{square.size}{Numeric value specifying the size of study effect squares. Default is 3.}

\item{contour_fill}{Fill colors for significance contours. Can be a vector of colors. Default is c("#E0E0E0", "#B0B0B0").}

\item{text_size}{Numeric value for text size in the plot. Default is 3.}

\item{xlim}{Numeric vector of length 2 specifying x-axis limits. Default is NULL (automatic).}

\item{pred}{Logical indicating whether to show prediction interval. Default is TRUE.}

\item{xpos}{Numeric value for horizontal position of text annotations. Default is 1.}

\item{study_x}{Numeric value specifying x-position for study labels. Default is NULL (automatic).}

\item{hetero_x}{Numeric value specifying x-position for heterogeneity stats. Default is NULL (automatic).}
}
\value{
A ggplot object representing the contour-enhanced forest plot.
}
\description{
This function generates a contour-enhanced forest plot for binary outcomes, such as Odds Ratios (OR) or Risk Ratios (RR), using data from intervention and control groups. The plot highlights statistical significance contours and optionally prediction intervals.
}
\examples{
forest.binary(dat)

}
