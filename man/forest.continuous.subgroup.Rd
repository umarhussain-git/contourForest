% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_subgroup_continuous.R
\name{forest.continuous.subgroup}
\alias{forest.continuous.subgroup}
\title{Create a Continuous Outcome Forest Plot with Subgroups}
\usage{
forest.continuous.subgroup(
  dat,
  m_t_col = "mean_t",
  sd_t_col = "sd_t",
  n_t_col = "n_t",
  m_c_col = "mean_c",
  sd_c_col = "sd_c",
  n_c_col = "n_c",
  subgroup_col = "subgroup",
  study_col = "Study",
  measure = "SMD",
  method = "REML",
  xlab = NULL,
  xlim = NULL,
  model = "RE",
  title = "Subgroup Forest Plot",
  diamond.col = "red",
  overall.col = "darkgreen",
  study.col.col = "blue",
  CI.col = "blue",
  Pred.Inter.col = "black",
  square.size = 5,
  Pred.Int.size = 1.5,
  text_size = 3.5,
  pred = TRUE,
  xpos = list(Study = NULL, MeanT = NULL, MeanC = NULL, Effect = NULL, Weight = NULL,
    PredInt = NULL, Hetero = NULL)
)
}
\arguments{
\item{dat}{A data frame containing the study data.}

\item{m_t_col}{Column name for the treatment group means (string).}

\item{sd_t_col}{Column name for the treatment group standard deviations (string).}

\item{n_t_col}{Column name for the treatment group sample sizes (string).}

\item{m_c_col}{Column name for the control group means (string).}

\item{sd_c_col}{Column name for the control group standard deviations (string).}

\item{n_c_col}{Column name for the control group sample sizes (string).}

\item{subgroup_col}{Column name indicating subgroup membership (string).}

\item{study_col}{Column name for study labels (string).}

\item{measure}{Effect measure: "SMD" (Standardized Mean Difference) or "MD" (Mean Difference) (string).}

\item{method}{Meta-analysis method for pooling: e.g., "REML", "DL" (string).}

\item{xlab}{Label for the x-axis (string).}

\item{xlim}{Numeric vector of length 2 giving x-axis limits, e.g., c(min, max) (default NULL for auto).}

\item{model}{Model type: "random" or "fixed" effects (string).}

\item{title}{Plot title (string).}

\item{diamond.col}{Color for the pooled effect diamond (string).}

\item{overall.col}{Color for overall pooled effect text/line (string).}

\item{study.col.col}{Color for study labels (string).}

\item{CI.col}{Color for confidence intervals (string).}

\item{Pred.Inter.col}{Color for prediction interval (string).}

\item{square.size}{Size of the study effect squares (numeric).}

\item{Pred.Int.size}{Size of the prediction interval diamond (numeric).}

\item{text_size}{Font size for annotations (numeric).}

\item{pred}{Logical; if TRUE, include prediction interval (default FALSE).}

\item{xpos}{List of x-axis positions for annotations (e.g., Study, Weight, Effect) (list).}
}
\value{
A ggplot object of the forest plot.
}
\description{
This function generates a forest plot for continuous outcomes, including study-specific means,
standard deviations, sample sizes, effect sizes (SMD or MD), subgroup analyses, and overall pooled effects.
}
\examples{
\dontrun{
df <- data.frame(
  Study = c("Study A", "Study B"),
  subgroup = c("Group 1", "Group 2"),
  mean_t = c(7.4, 5.9),
  sd_t = c(2.0, 1.9),
  n_t = c(50, 60),
  mean_c = c(5.2, 4.8),
  sd_c = c(1.8, 2.0),
  n_c = c(50, 60)
)
xpos <- list(Study=0, MeanT=1, MeanC=2, Effect=3, Weight=4)
forest.continuous.subgroup(df, m_t_col="mean_t", sd_t_col="sd_t", n_t_col="n_t",
                           m_c_col="mean_c", sd_c_col="sd_c", n_c_col="n_c",
                           subgroup_col="subgroup", study_col="Study",
                           measure="MD", method="REML", xlab="Mean Difference",
                           xlim=NULL, model="random", title="Forest Plot",
                           diamond.col="blue", overall.col="red", study.col.col="black",
                           CI.col="black", Pred.Inter.col="grey", square.size=3,
                           Pred.Int.size=4, text_size=3, pred=TRUE, xpos=xpos)
}
}
